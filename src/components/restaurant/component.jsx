import React from 'react';

export const Restaurant = ({restaurant}) => {

    if (!restaurant) {
        return null;
    }

    // пригодится для отдельного отображения

    return (
        <div>
            {/*<Menu menu={menu}/>*/}
        </div>
    );
};